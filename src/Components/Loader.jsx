import React from "react";
import { InfinitySpin } from "react-loader-spinner";

const Loader = () => {
  return (
    <div className="flex items-center justify-center h-screen text-green">
      render(
      <InfinitySpin width="200" color="#4fa94d" />)
    </div>
  );
};

export default Loader;
